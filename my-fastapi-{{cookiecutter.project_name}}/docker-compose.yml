services:
  app:
    hostname: abcde123_app
    container_name: abcde123_app
    profiles:
      - api
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - APP_ENV=development
    ports:
      - ${APP_PORT}:${APP_PORT}
    command: "python console.py run"
    healthcheck:
      test: ["CMD-SHELL", "python console.py check-health"]
      interval: 10s
      timeout: 60s
      retries: 5
      start_period: 10s
    env_file:
      - .env
    volumes:
      - ./src:/src
    networks:
      - app

networks:
  app:
    name: abcde123_app
    driver: bridge
